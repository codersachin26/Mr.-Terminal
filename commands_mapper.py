from cmd_func import *

commands ={}

commands['ls']     =   list_content
commands['cd']     =   change_dir
commands['mkdir']  =   make_dir
commands['cat']    =   read_file
commands['rm']     =   remove_file
commands['pwd']    =   pwd
commands['rmdir']  =   remone_dir
commands['cp']     =   copy_file